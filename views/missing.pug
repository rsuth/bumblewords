doctype html
html
  head
    meta(charset='UTF-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width, initial-scale=1.0')
    title bumblewords
    link(rel='stylesheet', href='/bumblewords.css')
    link(rel="icon" type='img/ico' href="/img/favicon.ico")
    link(rel="apple-touch-icon" type='img/png' href="/img/apple-touch-icon.png")
  body
    h1(class='noselect') bumblewords
    nav
      a(href='/leaderboard') back
    div
      p If you find a common english word (that is not a proper noun) that you think is missing from bumblewords please report it here and we will review it for inclusion.
      p Multiple words can be submitted - please seperate them with a space.
      p Words must be at least 4 characters long.
      if submitted
        p(style='color:green')
          | Thank you for your submission.
          | We will review it shortly.
        a(href='/') back to game
      else
        if error
          p(style="color: red;")
            | There was an error submitting your word.
            | Please try again.
        form(id="report-missing" action="/missing", method="post")
          label(for="word") word(s):   
          input(type="text", id="word" name="word", required, pattern="([a-zA-Z]{4,} ?)+", title="Words must be at least 4 characters, seperated by one space. No digits or special characters allowed.")
          | <br>
          input(type="submit", value="submit", style="margin-top: 10px;")
